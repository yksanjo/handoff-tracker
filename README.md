# handoff-tracker

Track ownership and handoffs between collaborating agents.

## Install

```bash
python3 -m venv .venv
source .venv/bin/activate
pip install -e .
```

## Usage

```bash
handoff-tracker --task "demo"
```
